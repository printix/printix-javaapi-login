spring:
  zipkin:
    enabled: false


# Authentication client configuration
printix:
  domain: devenv.printix.net
  oauth:
    client_id: javaapi-login-test
# Following properties can be used to costumize the oAuth configuration, but are normally not required
#    client_secret
#    grant_type
#    signin_uri
#    redirect_uri


# Following configuration should be supplied (overridden) to run the AuthenticationClientIntegrationTest
test:
  auth_client:
    trustAnySslCert: true
    tenantHostName: apitest
    otherTenantHostName: webtest
    adminUserName: apitestadmin@printix.net
    adminPassword: Qwerty1234
    globalAdminUserName: apitest@printix.net
    globalAdminPassword: Qwerty1234
    globalAdminTotpSecret: OWIUSZ2ZQTIB4BXM

    
logging:
  level:
    net.printix: TRACE
#    reactor.ipc.netty.channel.ChannelOperationsHandler: DEBUG
#    reactor.ipc.netty.http.server.HttpServer: DEBUG
    reactor.ipc.netty.http.client: DEBUG  # Setting this AND reactor.ipc.netty.channel to DEBUG to have full requests and responses logged.
    reactor.ipc.netty.channel: DEBUG 
#    io.reactivex.netty.protocol.http.client: DEBUG
#    io.netty.handler: DEBUG
#    io.netty.handler.proxy.HttpProxyHandler: DEBUG
#    io.netty.handler.proxy.ProxyHandler: DEBUG
#    org.springframework.web.reactive.function.client: DEBUG
    